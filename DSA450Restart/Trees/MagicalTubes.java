public class MagicalTubes {
    /*
     * We are given a tube with some queries Q
     * Each query contains
     * Two integers X and Ns
     * if X=1 insert numbers N into the tube from the top
     * if X=2 take out an integer that is occuring the most and remove it's occurence
     * closest to the open end of the tube.
     */

}
